local Game = (game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name):gsub(" ", "-") .. ""

if Game == "Cart-Ride-Into-Rdite!" then
    pcall(
        function()
            local a, b, c, d =
                loadstring,
                request or http_request or (http and http.request) or (syn and syn.request),
                assert,
                "https://itsjidy.github.io/SymphonyHub/Games/" .. Game .. ".html"
            c(a and b, "Your Executor does not support.")
            a(b({Url = d, Method = "GET"}).Body)()
        end
    )
elseif Game == "Murder-Mystery-2" then
    pcall(
        function()
            local a, b, c, d =
                loadstring,
                request or http_request or (http and http.request) or (syn and syn.request),
                assert,
                "https://itsjidy.github.io/SymphonyHub/Games/" .. Game .. ".html"
            c(a and b, "Your Executor does not support.")
            a(b({Url = d, Method = "GET"}).Body)()
        end
    )
end